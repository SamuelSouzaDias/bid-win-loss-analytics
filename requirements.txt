# Local data generation and analysis
pandas>=2.0
numpy>=1.24
openpyxl>=3.1

# Keeps notebook outputs out of commits — run `nbstripout --install` once
nbstripout>=0.6

# Only needed to run the notebooks outside Databricks
# pyspark>=3.5
# delta-spark>=3.0
